table! {
    picture (id) {
        id -> Integer,
        data -> Text,
        model -> Nullable<VarChar>,
        date -> Timestamp,
        latitude -> Nullable<VarChar>,
        longitude -> Nullable<VarChar>,
        thumb -> Text,
    }
}